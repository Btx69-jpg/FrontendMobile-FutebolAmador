package com.example.amfootball.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Define as constantes de cores básicas para a paleta de cores da aplicação.
 *
 * As cores com sufixo '80' são destinadas ao Dark Theme.
 * As cores com sufixo '40' são destinadas ao Light Theme.
 */

// --- Cores para Dark Theme (Modo Escuro) ---

/** Cor Primária clara (usada tipicamente em fundos escuros). */
val Purple80 = Color(0xFFD0BCFF)
/** Cor Secundária clara/Cinza-roxo (usada tipicamente em fundos escuros). */
val PurpleGrey80 = Color(0xFFCCC2DC)
/** Cor de Acento clara (usada tipicamente em fundos escuros). */
val Pink80 = Color(0xFFEFB8C8)

// --- Cores para Light Theme (Modo Claro) ---

/** Cor Primária escura (usada tipicamente em fundos claros). */
val Purple40 = Color(0xFF6650a4)
/** Cor Secundária escura/Cinza-roxo (usada tipicamente em fundos claros). */
val PurpleGrey40 = Color(0xFF625b71)
/** Cor de Acento escura (usada tipicamente em fundos claros). */
val Pink40 = Color(0xFF7D5260)